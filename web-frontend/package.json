{
  "name": "automo-client",
  "version": "1.0.0",
  "description": "Javascript client for AutoMO-Server.",
  "main": "src/index.js",
  "scripts": {
    "build": "browserify src/index.js -o ../app/static/scripts/bundle.js",
    "watch": "watchify src/index.js -o ../app/static/scripts/bundle.js --debug --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ali Aafee",
  "license": "",
  "dependencies": {
    "feather-icons": "^4.28.0",
    "focus-trap": "^5.1.0",
    "moment": "^2.26.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "watchify": "^4.0.0"
  }
}
